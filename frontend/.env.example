VITE_GITHUB_REPO=rasta-mmn/family-quest-rpg
VITE_GITHUB_BRANCH=main
# Optional override for local base path (default /family-quest-rpg/)
# VITE_BASE=/
# GitHub Contents API token is NOT an env var — set classic PAT in Admin UI (localStorage).
