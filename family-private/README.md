# Mapeamento privado (fora do jogo público)

Esta pasta está no `.gitignore`. Guarda aqui o significado real das missões e prémios.

Copie `mapping.example.md` → `mapping.md` e preencha. Nunca faça commit de `mapping.md`.
